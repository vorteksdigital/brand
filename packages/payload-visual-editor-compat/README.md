# Payload visual editor compatibility layer

Small Payload 3-safe boundary inspired by `pemedia/payload-visual-editor` 2.0.6.
Payload's official Live Preview supplies document transport and responsive Admin
UI. This package keeps origin validation, safe draft normalisation, and route
construction isolated for future replacement. See `docs/visual-editor.md`.
